---
name: CoS — Commitments and ambient digest
verb: commitments
tier: anchor
routine: follow-through.md + ambient digest
cron: "0 16 * * 5"
mode: autopilot
---

Load the `chief-of-staff` skill, then run `references/proactive.md` as tier: **anchor**,
routine: **commitment ageing** (`references/follow-through.md`).

Unattended mode — never call `ask_user`, nothing is sent, no obligation is confirmed or closed
without approval. Read `references/state-operations.md` and the canonical work ledger.
Prepare versioned nudge proposals in the action desk and hold them.

Check for silent resolution BEFORE ageing anything — being told to chase someone who already
replied is the fastest way to lose trust in this.

Then render the week's ambient digest from the queue: one section, capped at five items,
worst first.

The anchor alone leases the delivery batch. Persist the actual output and publication receipt
before acknowledging its included items. Record coverage separately per source, with failed
and incomplete resolution checks explicitly unknown and no successful checkpoint advancement.
